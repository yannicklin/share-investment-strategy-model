"""
DEPRECATED: Use app.bot.shared.models instead.
This file is kept for backward compatibility during migration but
now simply points to the shared market-isolated models.
"""

from app.bot.shared.models import Signal, ConfigProfile, ApiCredential, JobLog, db
