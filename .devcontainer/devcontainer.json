{
  "name": "ASX AI Trading System",
  "image": "mcr.microsoft.com/devcontainers/base:debian",

  "containerEnv": {
    "CURL_IMPERSONATE": "chrome131"
  },

  "features": {
    "ghcr.io/devcontainers/features/python:1": {
      "version": "3.12", "installTools": "false"
    },
    "ghcr.io/devcontainers-extra/features/uv:1": {},
    "ghcr.io/devcontainers/features/sshd:1": {}
  },
  
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "charliermarsh.ruff"
      ],
      "settings": {
        "python.defaultInterpreterPath": "${containerWorkspaceFolder}/.venv/bin/python3",
        "git.autofetch": true,
        "editor.formatOnSave": true,
        "editor.codeActionsOnSave": {
          "source.organizeImports": "explicit"
        },
        "[python]": {
          "editor.defaultFormatter": "charliermarsh.ruff"
        }
      }
    }
  },
  
  "forwardPorts": [8502],
  "portsAttributes": {
    "8502": {
      "label": "Streamlit App",
      "onAutoForward": "notify"
    }
  },
  
  "postCreateCommand": "bash -c 'echo export CURL_IMPERSONATE=chrome131 >> ~/.bashrc && make setup && pip install --upgrade curl-cffi --index-url https://pypi.org/simple && python -c \"from curl_cffi.const import CHROME_VERSIONS; print(\\\"✅ Supported Chrome versions:\\\", list(CHROME_VERSIONS.keys()))\" || echo \"⚠️  curl-cffi check skipped\"'",
  "postStartCommand": "git pull",
  
  "remoteUser": "vscode"
}
