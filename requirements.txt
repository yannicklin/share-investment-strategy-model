# Core AI and Data
# Synchronized with pyproject.toml
yfinance>=0.2.38
curl-cffi>=0.5.9  # For rate limit bypass (yfinance workaround)
pandas>=2.0.0
pandas_market_calendars>=4.3.0
numpy>=1.24.0
scikit-learn>=1.3.0
ngboost>=0.5.0
catboost>=1.2.0
prophet>=1.1.0
lxml # Required for pandas.read_html
# Standard Scikit-Learn algorithms (RandomForest, GradientBoosting) are built-in

# TensorFlow (OS-Specific)
tensorflow>=2.16.0; sys_platform != 'darwin' or platform_machine != 'arm64'
tensorflow-macos>=2.16.0; sys_platform == 'darwin' and platform_machine == 'arm64'
tensorflow-metal>=1.1.0; sys_platform == 'darwin' and platform_machine == 'arm64'

# UI and Visualization
streamlit>=1.30.0
plotly>=5.18.0

# Development and Testing
# These correspond to the [dependency-groups] dev in pyproject.toml
ruff
black
mypy
pytest
pytest-cov
