# Core AI and Data
yfinance>=0.2.38  # Taiwan stocks (.TW) - threads=False prevents chrome impersonation errors
FinMind  # Taiwan institutional data, margin trading, revenue
pandas_datareader  # Global indices (^SOX, ^IXIC) and USD/TWD
pandas
exchange_calendars>=4.5
pandas_market_calendars
numpy
scikit-learn
catboost
prophet
# Standard Scikit-Learn algorithms (RandomForest, GradientBoosting) are built-in

# TensorFlow (OS-Specific)
tensorflow; sys_platform != 'darwin' or platform_machine != 'arm64'
tensorflow-macos; sys_platform == 'darwin' and platform_machine == 'arm64'
tensorflow-metal; sys_platform == 'darwin' and platform_machine == 'arm64'

# UI and Visualization
streamlit
plotly

# Development and Testing
ruff
black
mypy
pytest
pytest-cov
