# Core AI and Data
yfinance
pandas
numpy
scikit-learn
# Use py-xgboost if available or standard with fallback
xgboost
catboost
prophet
# LightGBM is often a more portable alternative to XGBoost on Mac/ARM
lightgbm

# TensorFlow (OS-Specific)
tensorflow; sys_platform != 'darwin' or platform_machine != 'arm64'
tensorflow-macos; sys_platform == 'darwin' and platform_machine == 'arm64'
tensorflow-metal; sys_platform == 'darwin' and platform_machine == 'arm64'

# UI and Visualization
streamlit
plotly

# Development and Testing
ruff
black
mypy
pytest
pytest-cov
